git init
git remote add origin http://github.com/rdj020/xp-repo.git
git commit -m first1
git push origin master
git commit -a -m message
git add -A
git add -u #update
git checkout -b branchname
git branch # which branch am i in?
git checkout master #Ga naar branch master
git checkout branchname #Ga naar branch als ie al bestaat
